import React, { Component } from 'react';

class BehavioralDropdown extends Component {
  constructor(props) {
    super(props);

    this.state = { term: '' };
  }

  render() {
    return (

    );
  }
}

export default BehavioralDropdown;
