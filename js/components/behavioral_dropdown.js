import React from 'react';

class BehavioralDropdown extends React.Component {
  constructor(props) {
    super(props);

    this.state = { term: '' };
  }

  render() {
    return (

    );
  }
}

export default BehavioralDropdown;
